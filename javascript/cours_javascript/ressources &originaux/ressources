https://wikipedia.org
https://quiksmode.org
https://caniuse.com
https://thenounproject.com
http://www.editorializer.com
svg c'est langage HTML5  C4EST DES POINTSmathematique à la difference de bitmap squi est des grilles de pixels
FEUILLES DE STYLE DU NAVIGATEUR PAR DEFAUT
https://www3.org/CSS2/sample.html
on y trouve les particularitées  à retenir que le body a une margin:8px;
les h1 à h6 et p ont des marges top et bottom
les ul et ol ont des marges etb des padding-leftde 40px
on utlise un reset pour annuler les differences
le rem est=1x la valeur du font-size de la balise parent.ex:
h1=2em patr defaut h1 est enfant de la balise body on a donné une font-size de 14px alors h1 aura une taille de 2x 14
https://www.w3.org/TR/CSS2/propidx.html
intro javascript
le javascript(developpement coté client front-end developpement)est tout ce qui est traité par le navigateur et affiché à l'utilisateur(utilisateur)
ladev vs permet de reagir aux actions de l'utilisateur,directementds le navigateur.
Ajax:asynchronous Javascript & Xml,quin permet les changements sans reload de page
les qualité de dev:organisat°- rigueur-capacité d'abstraction-
! il faut être vigilant(se relir)un signe ecrit 2 fois au lieu d'1 peut tt changer.
il faut savoir fractioner les problémes pour réussir à solutioner ,garder une vue globale commême!!!
avt de commencer à écrire il faut être capable d'imaginer le resultat du code qu'il va ecrire.

La Version actuelle JS ,ES6 c'est une implementation nommé ecmaScript(standartisé par nescape en 1990)
il est inventé par Brendan Eich,cofondateur de MozillaDepuis 2009on a JS5 aujourd'hui c'est ES6 on peut definr une operation à clic
verifier desz données ua formulaire 
detecter desz changement de page 
stocker des valeurs utiles des variables
faire des operationsur des morceaux de text(chainede caractere)
permet executer du code en repionse à certains evenements se produisant sur une page web
créer ou declencher des animations,ds caroussel,etc.....
datavisualisation(histogramme cartgraphie,etc....)
application completers sur des bases de données(online ou offline)
JS est un langage stable et souple et compact.
les dev ont écrit de nbx outils sur le coeur de JS,créant des fonctions supplementaires 
Parmis ces outils il y a les API: apllication programming interface
 ,c'est la programation pr, navigateur
 Ce sont des block de code deja pret,ce qui aux dev de travaillé + facilement.
 c'est du code en kit.
 Ensuite API navigateur font partie integrante du navigateur,et accedent aux doonées de l'environnement informatique.
 ou faire des trucs complexes
 API DOM(doc objet model) ns permet de manipuler du html et du css (créer ou modifier du html ,appliquer de nouveau style à la page)
https://fr.wikipedia.org/wiki/Document_Object_Model#/media/File:DOM-model.svg
API de geolocalisation: permet de recuperer de données geographiques.
APIs Canvas et WebGL:permet de créer des graphiques 
https://developer.mozilla.org/fr/docs/Glossaire/Canvas
https://handbrake.fr
pour compression des fichiers videos.OGG/MP4/WebM
API video & audio permet de lire ss plugiing.
API WebRTC:peer2peer
API Web Storage:permet de faire des application offline(cache manifeste)
API web workers:muti thread en JS :
API de twitterpermet d'afficher les derniers tweet 
API googlemap pr les carte personnalise de regions
https://cloud.google.com
API tierces parties.
----------------------------------------------- SEPARATION DS CONCEPTES-----------------------------------------------



regles de bases:
-pas de css en JS
-pas de balise HTML5
- pas l'un ds l'autre (feuilles de style interne ou externe)
-on evite de travailler directement ds les lignes (pas de style inline)









